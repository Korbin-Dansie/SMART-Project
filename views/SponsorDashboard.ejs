<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sponsor Dashboard</title>
  <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
  <link rel="stylesheet" type="text/css" href="/font/bootstrap-icons.css">
  <%- include('partials/favico') %>
  <script type="text/javascript" src="crypto-js.js"></script>
</head>

<body>
  <div class="container-fluid vh-100" style="margin-top:10px">
    <div class="" style="margin-top:100px">
      <div class="rounded d-flex justify-content-center">
        <div class="col-md-4 col-sm-12 shadow-lg p-5 bg-light">
          <div class="row">
            <div class="col px-5">
              <button onclick="location.href = '/logout';" class="btn btn-primary mt-2 float-end">
                Logout
              </button>
            </div>
          </div>
          <div style="text-align:center"><h1>Sponsor Dashboard</h1></div>
          <h5 style="text-align:center;">Click on a student you are sponsoring to see more information</h5><br>
          <% if (locals.students.length > 0) { %>
            <table class="table table-striped table-hover table-sm">
              <thead>
                <tr>
                  <th scope="col">Student Name</th>
                </tr>
              </thead>
              <tbody>
                <% for (let i = 0; i < locals.students.length; i++) { %>
                  <tr>
                    <td><%= locals.students[i].first_name %> <%= locals.students[i].last_name %></td>
                    <td>
                      <form method="get" action="/student">
                        <input type="hidden" name="student_id" value=<%= locals.students[i].student_id %>>
                        <button class="btn btn-secondary" type="submit">View Report</button>
                      </form>
                    </td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          <% } else { %>
            No sponsored students found
          <% } %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>